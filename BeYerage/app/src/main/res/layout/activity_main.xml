<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/refresh_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:ignore="MissingClass">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <!--android:background="@color/colorAccent">-->

        <Button
            android:id="@+id/findBeverageButton"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:layout_margin="20dp"
            android:layout_weight="1"
            android:background="@drawable/button_selector"
            android:fontFamily="@font/font_koddi"
            android:text="원하는 음료 안내"
            android:textColor="@drawable/textcolor_selector"
            android:textSize="36sp" />

        <Button
            android:id="@+id/mostFreqBeverageButton"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:layout_margin="20dp"
            android:layout_weight="1"
            android:background="@drawable/button_selector"
            android:fontFamily="@font/font_koddi"
            android:text="추천 음료 안내"
            android:textColor="@drawable/textcolor_selector"
            android:textSize="36sp" />

        <Button
            android:id="@+id/closeConvenienceStoreButton"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:layout_margin="20dp"
            android:layout_weight="1"
            android:background="@drawable/button_selector"
            android:fontFamily="@font/font_koddi"
            android:text="근처 편의점 안내"
            android:textColor="@drawable/textcolor_selector"
            android:textSize="36sp" />
    </LinearLayout>

</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
